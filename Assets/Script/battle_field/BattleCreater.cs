/*
 * 用于生成battle data, 因为该方法可以重复调用且拥有一些序列化的内容
 *
 * 考虑到自己的想法有些问题，这个暂且搁置class
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleCreater : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
